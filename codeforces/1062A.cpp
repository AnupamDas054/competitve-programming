#include<bits/stdc++.h>

using namespace std;

int main()
{
    int x;
    cin>>x;
    int arr[1002];
    for(int i=0 ; i<x ; i++)
    {
        cin>>arr[i];
    }
    int cnt=0,mx=0;int sign=0;
    for(int i=0;i<x;i++)
    {
        if(i==0 && arr[i]==1 && arr[i+1]==)
    }
}

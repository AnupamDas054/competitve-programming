#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m,l,r;
    vector< pair<int,int> >v;
    for(int i=0;i<n;i++)
    {
        cin>>l>>r;
        v.push_back({l,(r-l)});N
    }
    sort(v.begin(),v.end());
    for(int i=0;i<=n;i++)
    {
       // k=v[i].second;
        k1=v[i].first
        if(j<k1)
        {
            j=v[i].first;
        }
        if(j<k)
        {
            j=v[i].second;
        }
    }
    return 0;
}

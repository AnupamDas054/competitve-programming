#include<bits/stdc++.h>

using  namespace std;

int main()
{
    string s;
    cin>>s;
    set<int>s1;
    map<char , int>m;
    vector<int>v;int s1=0;
    for(int i=0; i<s.size() ;i=i+2)
    {
       int k=s[i]-'0';
       v.push_back(k);

    }
    sort(v.begin(),v.end());
    for(int i=0;i<n;i++)
    {
        if(arr[i+1]-arr[i]==1)
        {
            v1.push_back(arr[i]);

        }
        else
        {
            if(v.size())
        }
    }

}
